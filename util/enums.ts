export enum Theme {
  LIGHT,
  DARK,
}

export enum ProjectType {
  WEB = "web",
  AI = "ai",
  SYSTEMS = "systems",
  ALGORITHMS = "algorithms",
  MOBILE = "mobile",
}

export enum Sections {
  HOME,
  PROJECTS,
  SKILLS,
}
